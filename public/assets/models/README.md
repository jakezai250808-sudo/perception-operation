# Dump Truck Model Assets

- 3D 优先路径：`dump_truck_tle.glb`
- 若缺失会自动回退到 `../dumptruck360/` 序列帧模式。

可执行：

```bash
node scripts/download-assets.mjs
```

并通过 `DUMP_TRUCK_MODEL_URL` 指定你们业务确认可用的矿卡 GLB 模型地址。
