# Dump Truck Model Assets

将矿卡模型放在：

- `public/assets/models/dump_truck.glb`

可用以下脚本下载默认演示模型（网络可用时）：

```bash
node scripts/download-assets.mjs
```

> 注意：默认下载的是公开可访问 GLB 演示模型。若要替换为更真实的非公路矿用宽体自卸车，请保持文件名不变并覆盖该路径。
